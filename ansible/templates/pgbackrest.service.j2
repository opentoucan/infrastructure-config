[Unit]
Description=PgBackRest for stanza {{ stanza_name }} repo {{ repo }} [{{ backup_type }}]
{% if pgbackrest_local_postgresql | bool %}
ConditionPathIsDirectory=/var/lib/pgsql/{{ postgresql_version }}/data
{% endif %}

[Service]
Type=simple
User={{ pgbackrest_owner }}
Group={{ pgbackrest_group }}
ExecStart=/usr/local/bin/pgbackrest-{{ stanza_name }}-{{ backup_type }}-repo{{ repo }}
